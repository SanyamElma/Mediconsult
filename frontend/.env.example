# Frontend environment configuration

# Use in-browser mock data (true) or the live Spring Boot backend (false)
VITE_USE_MOCK=true

# Backend base URL (used when VITE_USE_MOCK=false). Vite proxies /api to :8080 by default.
VITE_API_BASE_URL=/api
